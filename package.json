{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "engines": {
    "node": ">=22.0.0"
  },
  "packageManager": "pnpm@11.19.0",
  "scripts": {
    "preinstall": "node ./scripts/enforce-pnpm.mjs",
    "build": "pnpm run typecheck && pnpm --filter @workspace/tokencalc run build && wrangler deploy --dry-run",
    "deploy": "pnpm run build && wrangler d1 migrations apply llm-margin --remote && wrangler deploy",
    "dev": "wrangler dev",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm --filter @workspace/api-server run typecheck && pnpm --filter @workspace/tokencalc run typecheck"
  },
  "private": true,
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260822.1",
    "@types/node": "^25.3.3",
    "typescript": "~5.9.2",
    "prettier": "^3.8.1",
    "wrangler": "^4.125.0"
  }
}
